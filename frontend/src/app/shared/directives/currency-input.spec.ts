import { CurrencyInput } from './currency-input';

describe('CurrencyInput', () => {
  it('should create an instance', () => {
    const directive = new CurrencyInput();
    expect(directive).toBeTruthy();
  });
});
